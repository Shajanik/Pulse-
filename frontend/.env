# Base URL of the Go backend's HTTP API (no trailing slash).
VITE_API_BASE=https://pulse-stf8.onrender.com/

# Base URL of the Go backend's WebSocket endpoint (no trailing slash).
VITE_WS_BASE=ws://pulse-stf8.onrender.com/
